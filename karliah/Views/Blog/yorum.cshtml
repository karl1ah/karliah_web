@*@model deneme_projesi.Models.Yorum*@
@{
    ViewBag.Title = "yorum";
    
}

<h2>yorumunu yaz</h2>
@using (Html.BeginForm())
{
<div class="container">
    <div class="form-group">

        @Html.TextAreaFor(a => a, new { @class = "form-control" })
    </div>
    @Html.ActionLink("Yorum yap", "Details", "Blog", new { id = Model.Id }, new { @class = "btn btn-primary" })
    <button type="submit" class="btn btn-primary">Kaydet</button>
</div>
}

